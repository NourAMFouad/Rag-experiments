APP_NAME=Rag
APP_VERSION=0.1
OPENAI_API_KEY=""

FILE_ALLOWED_TYPES='["text/plain", "application/pdf"]'  
FILE_MAX_SIZE=10485760
FILE_DEFAULT_CHUNK_SIZE=512000 # 512KBs
DEBUG=True

# in pydentic we able to add the complex configuration like extenstions
# after add any thing in .env file 
# we do 2 steps 
# --> add them in .env_example 
# --> add datatype in config file 